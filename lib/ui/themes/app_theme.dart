import 'package:flutter/material.dart';

/// Theme constants and colors for the chess application
class AppTheme {
  static const backgroundColor = Colors.green;
  static const foregroundColor = Colors.white;
  static const selectedSquare = Colors.blueGrey;
  static const candidateSquare = Color.fromARGB(255, 94, 209, 255);
}

